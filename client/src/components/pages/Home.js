import '../../App.css';
import { Cards } from '../Cards';

import { HeroSection } from '../HeroSection';

export const Home = () => {
    return (
        <>
         <HeroSection />
         <Cards />
        </>
    );
}